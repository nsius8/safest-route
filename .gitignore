# Dependencies
node_modules/
# Lockfile may point at company registry; Render uses NPM_CONFIG_REGISTRY env var instead
package-lock.json

# Render: build output is created on deploy; env vars set in Render dashboard (do not commit .env)

# Build output
dist/
build/
*.tsbuildinfo

# Environment and secrets (never commit)
.env
.env.local
.env.*.local
.env.development
.env.production

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Vite
*.local

# Optional: test coverage (uncomment if you add tests)
# coverage/
